[project]
name = "backend"
version = "0.1.0"
description = "Fishing APP for Fishermen"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi[standard]>=0.129.0",
    "httpx>=0.28.1",
    "langgraph>=1.0.8",
    "pydantic-settings>=2.12.0",
    "uvicorn>=0.40.0",
]
[tool.fastapi]
entrypoint = "app.main:app"